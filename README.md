# AI Agent Case Gallery

Static gallery export for selected video replication cases.

- `index.html`: gallery entry page
- `case_gallery.html`: same gallery page, original filename
- `test_data/`: copied test inputs
- `outputs/`: copied generated outputs

Before publishing publicly, review `outputs/` for internal URLs, request payloads, logs, or sensitive user assets.
